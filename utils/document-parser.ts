import { Blob } from "buffer";

export async function extractTextFromDocument(blob: Blob): Promise<string> {
  // TODO: Implement document text extraction based on mime type
  // For now returning empty string to prevent errors
  return "";
}
